/// Punto de entrada del paquete `AppStarterKit`. Las Features viven bajo `Features/`
/// (generadas con `swift package generate-feature`, ver AGENTS.md).
public enum AppStarterKit {
    public static let version = "1.0.0"
}
