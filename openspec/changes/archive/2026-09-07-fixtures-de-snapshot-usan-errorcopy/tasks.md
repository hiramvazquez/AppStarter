# Tareas

- [x] 1. Los cuatro `vm.setError(...)` usan `ErrorCopy`, con `import Domain`
- [x] 2. `kit.conf` verifica también la app y sus targets de test, encadenados con `set -e`
- [x] 3. `/kit-verifica` en verde, y probado que sale rojo con un fichero roto
