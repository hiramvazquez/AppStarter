# kit.conf — qué significa "verificado" en AppStarter, y dónde vive el código.

# Dónde busca duplicados el detector. `AppTests` y `AppSnapshotTests` cuelgan de la raíz, no
# de `App/`, así que sin nombrarlos quedaban 18 ficheros Swift fuera. Medido el 2026-09-14 al
# añadirlos: cero grupos nuevos, así que no añade ruido.
FUENTES="App Packages AppTests AppSnapshotTests"

# Qué NO cubre esta firma. Viaja en la firma: el informe lo imprime y `/kit-estado` dice que
# está declarado.
LIMITES="- AppUITests quedan fuera a propósito: arrancan el simulador y tardan minutos, así que van
  en CI. Un cambio de UI (chrome, navegación, accesibilidad) NO está cubierto por esta firma.
- swift-format solo mira Sources y Tests de los dos paquetes, igual que CI: el formato de App/,
  AppTests, AppSnapshotTests y AppUITests no lo comprueba nadie (SwiftLint sí los recorre)."

verificaciones() {
    # Secretos, y el primero de todos: es lo más barato del informe (147 ms medidos) y lo que
    # no se puede deshacer si se cuela. Un token commiteado sigue en la historia aunque borres
    # la línea, así que hay que pararlo ANTES del commit, no revisarlo después.
    #
    # `--redact` para que un hallazgo no acabe impreso entero en el informe ni en la firma.
    # `dir` y no `git`: lo que la firma cubre es el ÁRBOL, que es donde está lo que se va a
    # commitear. La historia se escanea aparte, cuando haga falta (`gitleaks git`): hoy está
    # limpia, medido el 2026-09-22 sobre 128 commits.
    #
    # Es una dependencia más de verificar, como swiftlint: `brew install gitleaks`. Sin él el
    # paso sale en rojo, no se salta — un invariante que se salta solo no es un invariante.
    # `--ignore-gitleaks-allow` porque el comentario `// gitleaks:allow` es exactamente la
    # «desactivación suelta en el fichero afectado» que la spec `seguridad` prohíbe: sin este
    # flag, un token con ese comentario al lado pasaba en verde (medido). Una excepción va
    # declarada donde está la comprobación, no escondida junto al secreto.
    #
    # LÍMITE: queda otra salida, un `.gitleaksignore` en la raíz con la huella del hallazgo.
    # Está versionado y a la vista, así que no es «suelto», pero tampoco vive junto a esta
    # comprobación. Si aparece uno, que traiga escrito el motivo al lado.
    paso "secretos" gitleaks dir --config .gitleaks.toml --redact --ignore-gitleaks-allow --exit-code 1 .

    # SwiftLint --strict sobre TODO el repo, y el primero: es barato y así encabeza el informe.
    # Todo el repo por la misma razón que el paso de la app de abajo: CI solo lintea los
    # paquetes, así que lo que no se lintea aquí no lo mira nadie. Es una dependencia más de
    # verificar (`brew install swiftlint`, igual que CI): sin él, este paso sale en rojo, no se
    # salta. Los worktrees de `.claude/` quedan fuera por el `excluded` de `.swiftlint.yml`.
    paso "SwiftLint · --strict" swiftlint lint --strict --quiet

    # La orden de swift-format que CI ejecuta en cada paquete, carácter a carácter, y con su
    # mismo alcance (`Sources` y `Tests`) a propósito: fuera de ahí hoy hay avisos, y
    # ampliarlo obliga a decidir si se formatea eso, que es otro cambio. SwiftLint no cubre
    # esto — da cero violaciones sobre imports desordenados o líneas en blanco sobrantes. Un
    # paso por paquete, como los builds, para que cada uno enseñe su propia cola en el informe.
    paso "Platform · swift-format" bash -c 'cd Packages/Platform && swift format lint --strict --configuration ../../.swift-format --recursive Sources Tests'
    paso "Features · swift-format" bash -c 'cd Packages/Features && swift format lint --strict --configuration ../../.swift-format --recursive Sources Tests'

    paso "Platform · build"  bash -c 'cd Packages/Platform && swift build'
    paso "Platform · tests"  bash -c 'cd Packages/Platform && swift test'
    paso "Features · build"  bash -c 'cd Packages/Features && swift build'
    paso "Features · tests"  bash -c 'cd Packages/Features && swift test'

    # La app y sus tests NO son opcionales, y esto se aprendió por las malas: con solo los
    # cuatro pasos de arriba, `verifica.sh` firmaba VERDE teniendo un fichero de
    # AppSnapshotTests que ni siquiera era Swift válido — nada lo compilaba. Todo lo que
    # vive fuera de los paquetes SPM (App/, AppTests, AppSnapshotTests, AppUITests) queda
    # sin verificar si no se nombra aquí.
    #
    # Cuesta más tiempo, y es el precio de que la firma signifique lo que dice. Si quieres
    # el número, cronometra `/kit-verifica` — un dato escrito aquí caduca y nadie lo vuelve
    # a comprobar.
    # Un solo paso, con `set -e` A PROPÓSITO: separados, un build en rojo dejaba al segundo
    # corriendo contra el bundle ANTERIOR y estampando un ✅ sobre artefactos viejos. Un
    # informe con un verde falso dentro es peor que uno todo rojo.
    paso "App · build + AppTests + AppSnapshotTests" bash -c '
        set -e
        D="platform=iOS Simulator,name=iPhone 17"
        xcodebuild build-for-testing -project AppStarter.xcodeproj -scheme AppStarter \
            -destination "$D" -skipPackagePluginValidation CODE_SIGNING_ALLOWED=NO -quiet
        xcodebuild test-without-building -project AppStarter.xcodeproj -scheme AppStarter \
            -destination "$D" -only-testing:AppTests -only-testing:AppSnapshotTests \
            CODE_SIGNING_ALLOWED=NO -quiet'
}
